class Solution(object):
    def majorityElement(self, nums):
        """
        :type nums: List[int]
        :rtype: int
        """
        map = {}
        for i in nums:
            if i in map:
                map[i] += 1
            else:
                map[i] = 1
        maxval = -1
        max = 0
        for i in map:
            if map[i] > max:
                max = map[i]
                maxval = i

        return maxval