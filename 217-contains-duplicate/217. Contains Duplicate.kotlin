class Solution {
    fun containsDuplicate(nums: IntArray): Boolean {
        val Map = mutableMapOf<Int, Boolean>()
        for (num in nums) {
            if (Map.containsKey(num)) {
                return true
            }
            Map[num] = true
        }
        return false
    }
}