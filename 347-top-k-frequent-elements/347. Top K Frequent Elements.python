class Solution(object):
    def topKFrequent(self, nums, k):
        """
        :type nums: List[int]
        :type k: int
        :rtype: List[int]
        """
        nummap = {}
        for num in nums:
            if num in nummap:
                nummap[num] = nummap[num] + 1
            else:
                nummap[num] = 1
        sortedvalues = sorted(nummap, key=nummap.get, reverse=True)
        return sortedvalues[0:k]
